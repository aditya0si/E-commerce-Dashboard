# Runtime (pinned against the verified dev environment, Python 3.11)
streamlit==1.63.0
pandas==3.0.5
numpy==2.4.3
matplotlib==3.11.1
python-dotenv==1.2.2
# Tests
pytest==9.1.1
# Optional: PostgreSQL target only (default pipeline uses SQLite, no driver needed)
# sqlalchemy>=2.0; psycopg2-binary>=2.9
